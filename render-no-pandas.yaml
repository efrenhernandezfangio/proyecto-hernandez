services:
  - type: web
    name: site-survey-app-no-pandas
    env: python
    buildCommand: pip install -r requirements-no-pandas.txt
    startCommand: gunicorn app-no-pandas:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.18 